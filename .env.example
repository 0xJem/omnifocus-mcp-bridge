# .env is optional. These are the built-in defaults.
# Run `pnpm token:generate` to create .secrets/omnifocus-mcp-token.
# OMNIFOCUS_MCP_TOKEN_FILE=.secrets/omnifocus-mcp-token
# OMNIFOCUS_MCP_HOST=127.0.0.1
# OMNIFOCUS_MCP_PORT=3050
# OMNIFOCUS_MCP_READ_ONLY=true
# OMNIFOCUS_MCP_VERBOSE=false
